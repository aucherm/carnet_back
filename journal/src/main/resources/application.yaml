spring:
  application:
    name: fr_journal
  datasource:
    url: jdbc:postgresql://localhost:5432/carnet_lecture_super
    username: mymac
    password: 198208
  flyway:
    enabled: true
    location: classpath:db/migration
    baseline-on-migrate: true
    validate-on-migrate: true

server:
  port: 8081



security:
  jwt:
    secret-key: changeme
    expiration-time: 86400000  # 24h en millisecondes